RewriteEngine On

# Redirect pretty URLs to their corresponding .html files inside 'page/' folder
RewriteCond %{REQUEST_URI} !^/page/ [NC]
RewriteCond %{DOCUMENT_ROOT}/page/$1.html -f
RewriteRule ^([^/]+)$ /page/$1.html [L,R=302]

# Custom 404 Page
ErrorDocument 404 /404.html
